# Cash Drop

Juego Cash Drop independiente.

## Entrada

- `index.html` - juego principal.

## Uso

Abre `index.html` directamente en el navegador o despliega esta carpeta como proyecto estático.

La carpeta mantiene sus propias copias de `css/`, `js/` e `img/` para funcionar sin depender del proyecto original.
